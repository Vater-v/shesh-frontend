name: shesh
description: "The Shesh Frontend"
publish_to: 'none'

version: 1.0.0+1

environment:
    sdk: ^3.10.4

dependencies:
    flutter:
        sdk: flutter
    cupertino_icons: ^1.0.8
    shared_preferences: ^2.2.2

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^6.0.0

flutter:
    uses-material-design: true

    # Ассеты для приложения
    assets:
        - assets/

    # Шрифты (если понадобятся в будущем)
    # fonts:
    #   - family: Schyler
    #     fonts:
    #       - asset: fonts/Schyler-Regular.ttf
    #       - asset: fonts/Schyler-Italic.ttf
    #         style: italic
